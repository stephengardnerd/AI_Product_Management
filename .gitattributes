# Annotation tool preview layouts are reference artifacts, not source code.
# Exclude them from language detection so the card reflects the real work
# (Jupyter notebooks and React portfolio UI).
**/preview_layout_example*.html linguist-documentation
**/Instructions_Preview.html    linguist-documentation
